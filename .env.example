# =============================================================================
# Shopware 6 Admin API
# =============================================================================
# URL ohne trailing slash
SHOPWARE_URL=https://shop.markus-michalski.net

# Integration anlegen unter: Admin > Settings > System > Integrations
# Berechtigungen: product (read/write), category (read), property_group (read),
#                 manufacturer (read), tax (read), currency (read)
SHOPWARE_CLIENT_ID=SWIA...
SHOPWARE_CLIENT_SECRET=...

# =============================================================================
# Wiki.js Integration (fuer Software-Produkte)
# =============================================================================
# Base-URL ohne trailing slash
WIKIJS_BASE_URL=https://faq.markus-michalski.net

# =============================================================================
# Cache Settings (in Millisekunden)
# =============================================================================
# Kategorien und Properties aendern sich selten
CACHE_TTL_CATEGORIES=3600000
CACHE_TTL_PROPERTIES=3600000
# Snippets koennten oefter aktualisiert werden
CACHE_TTL_SNIPPETS=300000

# =============================================================================
# Logging
# =============================================================================
# Levels: debug, info, warn, error
LOG_LEVEL=info

# =============================================================================
# HINWEIS: Shop-spezifische IDs
# =============================================================================
# Die folgenden IDs sind in ProductService.ts hardcoded und muessen bei
# Verwendung in einem anderen Shop angepasst werden:
# - DEFAULT_TAX_ID (19% Steuersatz)
# - DEFAULT_CURRENCY_ID (EUR)
# Diese sollten idealerweise dynamisch via API abgerufen werden.
